{
  "name": "domestika",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "scripts": {
    "start": "npm run prebuild && parcel",
    "build": "parcel build",
    "prebuild": "rm -rf dist"
  },
  "browserList": "> 0.5%, last 2 versions, not dead",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0"
  }
}
